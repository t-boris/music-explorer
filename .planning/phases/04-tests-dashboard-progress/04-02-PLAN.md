---
phase: 04-tests-dashboard-progress
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/app/(auth)/progress/page.tsx, src/components/progress/skill-radar.tsx, src/components/progress/level-progress.tsx, src/components/progress/session-history.tsx, src/lib/progress-service.ts, src/hooks/use-progress.ts]
autonomous: true
---

<objective>
Build the progress tracking page with skill radar visualization, level completion percentage, and session history.

Purpose: Progress tracking closes the feedback loop — users see concrete evidence of improvement across 6 skill dimensions, motivating continued practice. The skill radar is the signature visualization.
Output: Progress page at /progress with skill radar chart, level progress bars, and practice session history timeline.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/types/index.ts
@src/lib/firebase.ts
@src/hooks/use-auth.ts
@src/app/(auth)/progress/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create progress service and build progress page</name>
  <files>src/lib/progress-service.ts, src/hooks/use-progress.ts, src/app/(auth)/progress/page.tsx, src/components/progress/skill-radar.tsx, src/components/progress/level-progress.tsx, src/components/progress/session-history.tsx</files>
  <action>
  **Progress service (src/lib/progress-service.ts):**
  "use client":
  - `getProgressData(userId)`: Fetches user doc for progressSummary, currentLevel
  - `getDetailedProgress(userId, skillType)`: Fetches `users/{uid}/progress/{skillType_levelId}` docs for drill-down
  - `getSessionHistory(userId, limit)`: Fetches practice sessions with limit, ordered by date desc
  - `updateSkillScore(userId, skillType, score)`: Updates progressSummary map field on user doc

  **useProgress hook (src/hooks/use-progress.ts):**
  "use client":
  - Uses useAuth() for userId
  - Real-time listener on user doc for progressSummary
  - Returns { progressSummary, currentLevel, sessions, loading }

  **Progress Page (/progress — replace placeholder):**
  "use client" page:
  - Page title "Your Progress"
  - Three sections: Skill Radar, Level Progress, Session History

  **SkillRadar component (src/components/progress/skill-radar.tsx):**
  Install: `npm install recharts` (if not already installed — check package.json first)
  - Radar/spider chart showing 6 skills: rhythm, intervals, chords, fretboard, technique, ear
  - Use Recharts RadarChart (NOT @nivo/radar — Recharts is already in deps or simpler to add)
  - Custom dark theme: surface-800 background, accent-400 fill with 30% opacity, accent-500 stroke
  - Axis labels in Inter font, skill names around perimeter
  - Score range 0-100 on each axis
  - If no scores yet: show empty radar with "Start practicing to see your skills!" message
  - Responsive: scales down on mobile

  **LevelProgress component (src/components/progress/level-progress.tsx):**
  - Shows current level and progress through levels 0-12
  - Horizontal progress bar: completed levels as accent fill, current level highlighted
  - Below bar: current level name and description
  - Lesson completion count: "3 of 3 lessons completed in Level 0"
  - Next level preview: what comes next

  **SessionHistory component (src/components/progress/session-history.tsx):**
  - Timeline/list of recent practice sessions (last 30)
  - Each entry: date, duration, exercises practiced, recording count badge
  - Weekly/monthly grouping headers
  - Total practice time summary at top
  - Link to each session detail

  All components: dark themed, accent colors for data visualization.
  </action>
  <verify>npm run build passes. /progress shows skill radar, level progress, and session history. Recharts radar renders correctly.</verify>
  <done>Progress page with skill radar visualization, level progress tracking, and session history timeline. All data from Firestore with graceful empty states.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] /progress shows skill radar chart (or empty state)
- [ ] Level progress bar renders with current level
- [ ] Session history shows recent sessions
- [ ] Recharts radar chart uses dark theme colors
- [ ] Responsive layout works
- [ ] No TypeScript errors
</verification>

<success_criteria>
- All tasks completed
- Skill radar is the signature visualization (dark, accent-colored)
- Progress page provides comprehensive view of learning journey
- Empty states are encouraging, not blank
</success_criteria>

<output>
After completion, create `.planning/phases/04-tests-dashboard-progress/04-02-SUMMARY.md`
</output>
