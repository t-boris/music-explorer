---
phase: 04-tests-dashboard-progress
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/(auth)/dashboard/page.tsx, src/components/dashboard/today-plan.tsx, src/components/dashboard/quick-record.tsx, src/components/dashboard/streak-display.tsx, src/components/dashboard/progress-overview.tsx, src/lib/dashboard-service.ts, src/hooks/use-dashboard.ts]
autonomous: true
---

<objective>
Build the personal dashboard — the user's home after sign-in, showing today's plan, quick record, streak, and progress overview.

Purpose: The dashboard is the daily landing page that motivates practice. It surfaces what to work on, provides quick access to recording, and shows streak/progress to maintain motivation.
Output: Dashboard at /dashboard with today's plan, quick record widget, streak display, and progress summary.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/types/index.ts
@src/lib/firebase.ts
@src/hooks/use-auth.ts
@src/lib/practice-service.ts
@src/components/recording/audio-recorder.tsx
@src/app/(auth)/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard service and data hook</name>
  <files>src/lib/dashboard-service.ts, src/hooks/use-dashboard.ts</files>
  <action>
  **Dashboard service (src/lib/dashboard-service.ts):**
  "use client" — aggregates data for dashboard:
  - `getDashboardData(userId)`: Fetches in parallel:
    1. User document (currentLevel, progressSummary, streakDays, totalPracticeMinutes)
    2. Recent practice sessions (last 7 days)
    3. Recent recordings count (last 7 days)
    4. Recent tempo attempts (last 7 days)
  - Returns typed DashboardData object
  - Add DashboardData type to types/index.ts:
    ```typescript
    interface DashboardData {
      user: User | null;
      recentSessions: PracticeSession[];
      recentRecordingsCount: number;
      recentTempoAttempts: number;
      todaySession: PracticeSession | null;
      currentStreak: number;
      totalMinutes: number;
      weeklyMinutes: number;
    }
    ```

  **useDashboard hook (src/hooks/use-dashboard.ts):**
  "use client" — loads dashboard data:
  - Uses useAuth() to get userId
  - Calls getDashboardData on mount and on userId change
  - Returns { data, loading, error }
  - Optionally: real-time listener on user doc for streak updates
  </action>
  <verify>TypeScript compiles. DashboardData type added to types/index.ts.</verify>
  <done>Dashboard data service aggregates user stats, sessions, and activity metrics.</done>
</task>

<task type="auto">
  <name>Task 2: Build dashboard page with widgets</name>
  <files>src/app/(auth)/dashboard/page.tsx, src/components/dashboard/today-plan.tsx, src/components/dashboard/quick-record.tsx, src/components/dashboard/streak-display.tsx, src/components/dashboard/progress-overview.tsx</files>
  <action>
  **Dashboard Page (/dashboard — replace placeholder):**
  "use client" page using useDashboard hook:
  - Grid layout: 2 columns on desktop, single column on mobile
  - Greeting: "Welcome back, {name}" with current date
  - Widget grid below greeting

  **TodayPlan widget (src/components/dashboard/today-plan.tsx):**
  - Card showing: "Today's Practice"
  - If today's session exists: show duration, exercises practiced, "Continue" link
  - If no session today: "Start today's session" CTA linking to /practice/new
  - Show current level and suggested next lesson to study
  - Surface-800 card with accent border on left

  **QuickRecord widget (src/components/dashboard/quick-record.tsx):**
  - Compact AudioRecorder embed (reuse from Phase 3)
  - contextType="free" for quick recordings not attached to exercises
  - "Quick Record" header with mic icon
  - Minimal UI: just the record button and timer

  **StreakDisplay widget (src/components/dashboard/streak-display.tsx):**
  - Large streak number in Space Grotesk (e.g., "14 days")
  - Fire/flame icon from Lucide (Flame)
  - Total practice minutes below
  - Weekly minutes bar (this week vs last week, simple comparison)
  - Accent color for streak number, warm amber glow effect

  **ProgressOverview widget (src/components/dashboard/progress-overview.tsx):**
  - Mini skill summary from user.progressSummary
  - 6 skills as horizontal bars (rhythm, intervals, chords, fretboard, technique, ear)
  - Each bar: skill name, score as percentage bar with accent fill
  - "View Full Progress" link to /progress
  - Current level indicator: "Level {n}: {title}"
  - This is a simplified preview — full radar chart in 04-03

  All widgets: dark surface-800 cards, consistent spacing, responsive grid.
  </action>
  <verify>npm run build passes. /dashboard shows greeting, today's plan, quick record, streak, and progress overview widgets. Responsive layout works on mobile.</verify>
  <done>Dashboard page with 4 widgets: today's plan, quick record, streak display, progress overview. Dark themed, responsive, motivational.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] /dashboard shows personalized greeting
- [ ] Today's plan widget shows session status or CTA
- [ ] Quick record widget embeds AudioRecorder
- [ ] Streak display shows days and minutes
- [ ] Progress overview shows 6 skill bars
- [ ] Responsive layout (2-col desktop, 1-col mobile)
- [ ] All TypeScript compiles
</verification>

<success_criteria>
- All tasks completed
- Dashboard is a motivating daily landing page
- All widgets use real Firestore data (or graceful empty states)
- Dark studio aesthetic consistent
</success_criteria>

<output>
After completion, create `.planning/phases/04-tests-dashboard-progress/04-01-SUMMARY.md`
</output>
