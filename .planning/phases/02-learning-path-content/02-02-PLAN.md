---
phase: 02-learning-path-content
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/app/(public)/songs/page.tsx, src/app/(public)/songs/[songId]/page.tsx, src/components/content/song-card.tsx, src/components/content/song-detail.tsx, content/songs/]
autonomous: true
---

<objective>
Build song/composition browsing pages where each song is tagged with theory concepts it demonstrates.

Purpose: Songs bridge abstract theory to real playing. Users browse songs, see which theory concepts apply, and find derived exercises. Public, no auth required.
Output: Song list page at /songs with filtering, song detail page at /songs/[songId] showing theory tags, difficulty, linked exercises.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/types/index.ts
@src/components/content/theory-tag.tsx
@src/components/content/level-card.tsx
@src/lib/content.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create song content structure and data utilities</name>
  <files>content/songs/, src/lib/content.ts</files>
  <action>
  Create song content as MDX files (same pattern as lessons):

  ```
  content/
  └── songs/
      ├── smoke-on-the-water.mdx
      ├── come-as-you-are.mdx
      └── seven-nation-army.mdx
  ```

  Each song MDX file has frontmatter:
  ```yaml
  title: "Smoke on the Water"
  artist: "Deep Purple"
  difficulty: 1
  tags: ["intervals", "power-chords", "rhythm"]
  levelIds: ["level-2", "level-4"]
  description: "Classic riff demonstrating perfect fourths and power chord movement"
  ```

  Body contains: brief analysis of the song's theory elements, key musical concepts, suggested practice approach.

  Add to src/lib/content.ts:
  - `getSongs()` — returns all songs sorted by difficulty
  - `getSong(songSlug)` — returns single song with MDX content
  - `getSongsByTag(tag)` — filter songs by theory tag
  - `getSongsByLevel(levelId)` — filter songs linked to a level

  Create 3 sample songs with real musical analysis (public domain riffs or well-known riffs with educational commentary, no copyrighted tabs).
  </action>
  <verify>content.ts getSongs() returns 3 songs. getSongsByTag("intervals") returns relevant songs.</verify>
  <done>Song content files created, content utility functions work, songs have meaningful frontmatter and theory analysis.</done>
</task>

<task type="auto">
  <name>Task 2: Build song list and song detail pages</name>
  <files>src/app/(public)/songs/page.tsx, src/app/(public)/songs/[songId]/page.tsx, src/components/content/song-card.tsx</files>
  <action>
  **Song List (/songs):**
  - Server component calling getSongs()
  - Page title "Compositions & Riffs" with subtitle
  - Filter bar: filter by difficulty (1-5 stars) and by theory tag
  - For MVP: simple tag pills that act as filters (client component for interactivity)
  - Grid layout of SongCard components
  - Empty state if no songs match filter

  **SongCard component:**
  - Card with surface-800 bg, hover effect
  - Song title (Space Grotesk), artist name (text-secondary)
  - Difficulty as visual indicator (dots or stars, accent colored)
  - Theory tags as TheoryTag pills (reuse from 02-01)
  - Linked levels as small badges
  - Click navigates to song detail

  **Song Detail (/songs/[songId]):**
  - Server component with getSong(songId)
  - Header: title, artist, difficulty, all tags
  - Body: MDX content rendered with same mdxComponents from 02-01
  - Sidebar or section: "Theory Concepts" — list of linked levels with links back to /levels/[levelId]
  - Section: "Related Exercises" — if any exercises reference this song (future), show them
  - Breadcrumb: Songs > Smoke on the Water

  Use the same MDX rendering approach (next-mdx-remote) as lesson pages.
  </action>
  <verify>/songs shows 3 song cards with tags and difficulty. Click into a song — see theory analysis rendered as MDX. Filter by tag works.</verify>
  <done>Song list page with filtering, song detail with MDX rendering, consistent dark theme, theory tags link back to levels.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] /songs shows all 3 sample songs
- [ ] Song cards display title, artist, difficulty, theory tags
- [ ] /songs/[songId] renders MDX content with correct styling
- [ ] Tag filtering works on song list
- [ ] Theory tags on song detail link to relevant levels
- [ ] Dark theme consistent, typography matches design system
</verification>

<success_criteria>
- All tasks completed
- Song browsing works end-to-end (list → detail)
- Theory tags connect songs to levels
- MDX rendering consistent with lesson pages
</success_criteria>

<output>
After completion, create `.planning/phases/02-learning-path-content/02-02-SUMMARY.md`
</output>
